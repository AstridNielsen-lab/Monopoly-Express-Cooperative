<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        tools:targetApi="31">

        <!-- Splash Screen como Activity Principal -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/SplashTheme"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Login Activity -->
        <activity
            android:name=".LoginActivity"
            android:exported="false"
            android:theme="@style/AppTheme"
            android:screenOrientation="portrait" />
        
        <!-- MainActivity (antiga principal) -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".ClienteActivity"
            android:exported="false"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".MotoboyActivity"
            android:exported="false"
            android:theme="@style/AppTheme" />
            
        <!-- Cliente Dashboard -->
        <activity
            android:name=".ClienteDashboardActivity"
            android:exported="false"
            android:theme="@style/AppTheme" />
            
        <!-- Solicitar Entrega -->
        <activity
            android:name=".SolicitarEntregaActivity"
            android:exported="false"
            android:theme="@style/AppTheme" />
            
        <!-- Motoboy Mapa -->
        <activity
            android:name=".MotoboyMapaActivity"
            android:exported="false"
            android:theme="@style/AppTheme" />
            
        <!-- Histórico -->
        <activity
            android:name=".HistoricoActivity"
            android:exported="false"
            android:theme="@style/AppTheme" />
            
        <!-- Entregas em Andamento -->
        <activity
            android:name=".EntregasAndamentoActivity"
            android:exported="false"
            android:theme="@style/AppTheme" />
            
        <!-- Perfil -->
        <activity
            android:name=".PerfilActivity"
            android:exported="false"
            android:theme="@style/AppTheme" />

    </application>

</manifest>

